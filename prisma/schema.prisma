generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model ShoppingItem {
  id      String  @id @default(cuid())
  name    String
  checked Boolean @default(false)
}
